# ysb-wholesale-scraper 使用指南

> 采集药师帮（dian.ysbang.cn）批发商品数据，破解价格反爬，生成可排序、去重的 Excel + HTML 统计报表。
>
> **本 skill 由 AI agent（如 TRAE / WorkBuddy）自动调用，用户只需用自然语言描述需求即可，无需手动执行任何命令。**

---

## 一、功能概述

| 能力 | 说明 |
|------|------|
| 价格破解 | 两层反爬：字体反爬（DOM乱码）+ 数据层加密（priceToken protobuf），用解码绕过 |
| 销量采集 | 列表页「已拼」销量 + 详情页「已付款件数」权威销量 |
| 深度数据 | 近7天销量、大单统计(≥50)、采购记录时序、阶梯价、店参团数 |
| 自动登录 | CDP 直连浏览器，网易易盾滑块自动解决（图像分析+模拟拖拽） |
| 滑块处理 | 采集全程3处检测点，先自动解决（JS canvas分析），失败转人工等待 |
| 智能路由 | 拼团商品(包邮)和普通商品(起购)自动选择正确路由参数 |
| 数据归并 | 按商品名+规格去重，按产品系列归并不同规格 |
| 诚实降级 | 抓取失败的行置灰标「待补采」，不伪造数据 |

---

## 二、使用方法

### 2.1 基本用法

直接告诉 AI 你要采集什么品牌、抓几页：

| 用户说 | AI 自动执行 |
|--------|------------|
| "帮我采集云南白药牙膏的数据，只抓第1页" | 登录检测 → 列表采集(1页) → 详情页采集 → 出表 |
| "采集汤臣倍健，抓5页" | 登录检测 → 列表采集(5页) → 详情页采集 → 出表 |
| "帮我看看同仁堂的批发价格" | 登录检测 → 列表采集 → 详情页采集 → 出表 |

### 2.2 AI 执行流程

AI 收到请求后，会自动按以下流程执行（用户无需干预）：

```
run.py 一键全流程编排
├─ 1. 检查 Chrome 9222 是否可用
│   ├─ 不可用 → 提示用户启动 Chrome 调试会话
│   └─ 可用 → 继续
│
├─ 2. 检查登录状态
│   ├─ 未登录 → 自动登录+滑块验证（auto_login.py）
│   │          ├─ 滑块通过 → 继续
│   │          └─ 滑块失败 → 提示用户在 Chrome 窗口手动完成
│   └─ 已登录 → 继续
│
├─ 3. 列表采集（cdp_extract.py，CDP直连，推荐）
│   → 输出 vuex_raw.json
│   → 途中弹滑块：先自动解决，失败提示用户手动完成
│   → (备选: extract.py，browser-use 沙箱，需 Python ≥3.11)
│
├─ 4. 详情页采集（cdp_fetch_detail_v2.py，CDP直连，推荐）
│   → 输出 detail_data.json
│   → 断点续传，崩溃可重跑续上
│   → 途中弹滑块：3处检测点，先自动解决再转人工
│   → (备选: fetch_detail.py，browser-use 沙箱，需 Python ≥3.11)
│
├─ 5. 解析出表（process.py，普通 Python）
│   → 生成 Excel + HTML 报表
│   → 自动记录成长数据到 profile.json
│
└─ 6. 交付文件给用户
    → Excel: 品牌_热销统计_系列合并_日期.xlsx
    → HTML: 品牌_热销采购分析_日期.html
```

> **一条命令完成全流程**：AI 通过 `run.py` 自动串联所有步骤，无需手动执行多条命令。`run.py` 自动检测 browser-use 可用性，不可用时回退到 CDP 直连模式（兼容 Python 3.10+）。

### 2.3 常见使用场景

**只抓列表不出详情（快速看价格和排名）**
> "帮我快速看一下仁和的批发价格，不用抓详情页"

AI 只执行列表采集 + 出表，跳过详情页步骤。权威销量列显示「—」。

**只补采详情（列表已抓过，想补全销量数据）**
> "之前抓的云南白药牙膏详情页没抓完，帮我补全一下"

AI 用已有的 vuex_raw.json + detail_data.json 断点续传，只抓缺失的详情页。

**换品牌采集**
> "再帮我采集一个妇炎洁的"

AI 自动换品牌参数，完整跑一遍流程。成长记录会自动累积到 profile.json。

**查看历史趋势**
> "帮我看看云南白药牙膏的历史采集趋势"

AI 运行 `grow.py --brand 云南白药牙膏 --trend`，展示销量/价格变化。

**查看数据质量**
> "上次采集的数据质量怎么样？"

AI 运行 `grow.py --quality`，展示采集成功率、待补采数量及变化趋势。

### 2.4 需要用户介入的情况

大多数情况下全自动，以下场景需要用户配合：

| 场景 | 用户需要做什么 |
|------|---------------|
| 首次使用 | 启动 Chrome 调试会话（AI 会给出命令） |
| 自动登录滑块验证失败 | 在弹出的 Chrome 窗口中手动拖动滑块 |
| 采集途中滑块自动解决失败 | 同上，手动完成后脚本自动继续 |
| 登录过期 | 提供账号密码，AI 重新自动登录 |

### 2.5 AI 调用时的参数传递

AI 会根据用户描述自动设置参数，无需用户手动指定：

| 用户描述 | AI 自动设置的参数 |
|----------|-------------------|
| "云南白药牙膏" | `--brand 云南白药牙膏` |
| "抓3页" | `--pages 3` |
| "只看第1页" | `--pages 1` |
| "按销量排序" | `--sort-by-sales true`（默认） |
| "补全详情" | `--existing detail_data.json`（断点续传） |
| "只抓前10个详情" | `--top-n 10` |

### 2.6 输出文件交付

AI 完成采集后，会向用户交付以下文件：

| 文件 | 内容 |
|------|------|
| `品牌_热销统计_系列合并_日期.xlsx` | 三张表：热销产品排行 / TOP10最低价档位 / 产品明细 |
| `品牌_热销采购分析_日期.html` | 可视化报表（含数据完整性状态条、阶梯价、近7天/大单列） |
| `sales_history.json` | TOP10快照（供趋势分析，自动累积） |

AI 会用 `computer://` 链接把文件分享给用户，用户可直接点击打开。

---

## 三、前置准备

### 3.1 唯一需要用户做的事：启动 Chrome 调试会话

AI agent 需要通过 Chrome 调试端口（9222）控制浏览器。首次使用时，用户需要启动一个带调试端口的 Chrome：

```bash
chrome.exe --remote-debugging-port=9222 --remote-allow-origins=* --user-data-dir=你的持久目录 --no-first-run
```

- 端口 9222 用于 browser-use 和 CDP 直连
- `--user-data-dir` 持久化登录态，避免每次重新登录
- `--remote-allow-origins=*` 必须，否则 WebSocket 会被拒绝

> 启动后把这个 Chrome 窗口保持打开即可，后续所有操作由 AI agent 自动完成。

### 3.2 依赖安装（AI 自动处理）

以下依赖由 AI agent 在运行时自动检测并安装，用户无需关心：

| 依赖 | 用途 | 必需性 |
|------|------|--------|
| openpyxl | Excel 生成 | 必需 |
| websocket-client | CDP 直连浏览器（列表/详情/登录） | 必需 |
| Pillow + numpy | 滑块图像分析 | 必需 |
| ddddocr | 滑块缺口精准匹配 | 推荐 |
| browser-use | browser-use 版采集脚本（备选） | 可选（需 Python ≥3.11） |

---

## 四、技术架构（AI 内部执行参考）

> 以下内容供了解 AI agent 内部如何工作，用户无需手动执行任何命令。

### 4.1 工作流总览

```
run.py (一键全流程编排)
  │
  ├─ 步骤0: 自动登录          步骤1: 列表采集           步骤2.5: 详情页采集         步骤2: 解析出表
  │  auto_login.py     →     cdp_extract.py    →      cdp_fetch_detail_v2.py →  process.py
  │
  │  手机号+密码              按关键词搜索             逐个商品详情页               合并列表+详情
  │  CDP直连浏览器            逐页读Vuex Store         SPA内Vue Router导航         解码价格Token
  │  网易易盾滑块验证          按商品名匹配卡片          正则抽取权威销量             按系列归并去重
  │  (图像分析+模拟拖拽)       滑块自动处理              智能路由(拼团/普通)          生成Excel+HTML
  │                          翻页点"下一页"            滑块自动处理
  │
  └─ (备选: extract.py)       (备选: fetch_detail.py)
     browser-use 沙箱          browser-use 沙箱
```

> **公共模块**：`ysb_common.py`（CDP通信+滑块验证）和 `ysb_parser.py`（价格解码+销量解析+JS模板）被所有采集脚本调用。

### 4.2 步骤 0 — 自动登录 + 滑块验证

**脚本**：`scripts/auto_login.py`
**触发条件**：Chrome 9222 会话未登录或登录过期时，AI 自动运行

**工作原理**：
1. CDP 直连浏览器，自动填写账号密码
2. 检测网易易盾滑块出现
3. 下载背景图，用 numpy 分析每列像素梯度找到缺口边缘
4. 模拟人类拖拽轨迹（ease-out 先快后慢 + y轴抖动 + 终点回弹）
5. 失败自动刷新验证码重试，最多 4 次
6. 退出码 0=成功，1=失败（需人工处理）

> 若已登录则直接退出，不重复登录。滑块验证实测第3次尝试通过率最高。

### 4.3 步骤 1 — 列表页采集

**推荐脚本**：`scripts/cdp_extract.py`（CDP直连，兼容 Python 3.10+）
**备选脚本**：`scripts/extract.py`（browser-use 沙箱，需 Python ≥3.11）
**输出**：`vuex_raw.json`

`run.py` 自动检测 browser-use 可用性，不可用时使用 CDP 直连版。两个脚本功能一致，参数相同。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--brand` | 云南白药 | 品牌名/搜索关键词 |
| `--pages` | 1 | 抓取页数（每页约 60 条） |
| `--sort-by-sales` | true | 是否按销量排序 |

**关键机制**：
- 翻页靠点「下一页」按钮（`.pagination-next`），URL 的 `?page=N` 对 SPA 无效
- 列表稳定后才读取（防瞬间空列表误读）
- 按商品名精确匹配 DOM 卡片（避免「猜你喜欢」推荐商品串号）
- 采集途中弹出滑块：先自动解决（调用 `ysb_parser.handle_verify_browser`），失败转人工等待

### 4.4 步骤 2.5 — 详情页权威销量采集

**推荐脚本**：`scripts/cdp_fetch_detail_v2.py`（CDP直连，集成 ddddocr，兼容 Python 3.10+）
**备选脚本**：`scripts/fetch_detail.py`（browser-use 沙箱，需 Python ≥3.11）
**输出**：`detail_data.json`

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--input` | vuex_raw.json | 步骤1的输出 |
| `--existing` | detail_data.json | 已有详情路径，用于断点续传 |
| `--top-n` | 0 | 只抓前 N 个（0=全量） |

**采集字段**：

| 字段 | 来源 | 说明 |
|------|------|------|
| 成团价/采购价 | 详情页 | 真实单价 |
| 折后价 | 详情页 | 普通页有 |
| 已付款件数 | 详情页 | **权威销量主指标** |
| 参团店铺数 | 详情页 | 拼团页有 |
| 采购笔数 | 详情页 | 采购记录总数 |
| 近7天销量 | 采购记录筛选 | 按时间戳筛选最近7天 |
| 大单统计(≥50) | 采购记录筛选 | 单笔≥50的笔数+总销量 |
| 有效期/生产日期/厂家/批号 | 详情页 | 资质信息 |

**关键机制**：
- SPA 内 Vue Router 导航（`$router.push`），不刷新页面
- 每次导航前先回 `/home` 清除 SPA 缓存（强制 Vue 卸载组件）
- 智能路由选择：
  - 拼团商品（包邮）→ 先 `isAssemble=true, scene=0`
  - 普通商品（起购）→ 先 `isAssemble=false, scene=1`
  - 失败自动尝试另一种路由
- 断点续传：崩溃后重跑自动跳过已抓的，自动重试空数据记录
- 商品间隔 2 秒防验证
- 3 处滑块检测点：SPA 入口页、每次导航后、new_tab 兜底后

### 4.5 步骤 2 — 解析与出表

**脚本**：`scripts/process.py`（普通 Python 运行）

AI 根据用户描述自动设置品牌名等参数，脚本解析采集数据并生成报表。

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--brand` | 妇炎洁 | 品牌名（文件命名/标题/前缀剥离） |
| `--input` | vuex_raw.json | 步骤1的输出 |
| `--detail` | detail_data.json | 步骤2.5的输出（无则权威销量列显示「—」） |
| `--img-dir` | ysb_images | 图片目录 |
| `--output-xlsx` | 自动生成 | 显式指定 xlsx 路径 |
| `--output-html` | 自动生成 | 显式指定 html 路径 |

**处理逻辑**：
1. 解码 `priceToken`（base64 protobuf）得真实单价
2. 解析「已拼N」销量（支持「N万+」×10000）
3. 按商品名+规格去重，再按产品系列归并
4. 整合列表 + 详情数据，用「已付款件数」作权威销量主指标
5. 商品链接根据商品名自动选择拼团/普通路由参数
6. 阶梯价：同一产品下所有 `(起订量, 单价, 单位)` 组合按起订量升序

### 4.6 browser-use 文件系统隔离

browser-use 沙箱进程写出的文件在真实工作区不可见。AI agent 的处理方式：采集脚本只把 JSON 打到 stdout，由普通 Python 进程负责写 xlsx / 图片 / html。

> **`run.py` 已自动处理此问题**：它用 `subprocess` 捕获 stdout 后由主进程写入文件，不依赖 shell 重定向（避免 PowerShell 编码问题）。仅在单独执行 browser-use 版脚本时需注意此限制。

---

## 五、输出文件

| 文件 | 说明 |
|------|------|
| `品牌_热销统计_系列合并_日期.xlsx` | 三张工作表：热销产品 / TOP10最低价档位 / 产品明细 |
| `品牌_热销采购分析_日期.html` | 网页版报表（含数据完整性状态条、阶梯价、近7天/大单列、档位页可钻取） |
| `sales_history.json` | 每日 TOP10 快照，供趋势分析 |
| `ysb_images/` | 下载的商品图片 |

### Excel 三表结构

**热销产品表**：
排名 / 产品名称 / 规格 / 单价 / 起订量 / 总销量 / 供应商数 / 全网最低价 / 阶梯价(所有档位) / 有效期 / 商品ID(可点击跳转详情) / 近7天销量 / 大单数(≥50) / 大单总销量

**TOP10最低价档位表**：
产品内 3 个最低报价（含价格/起订/供应商/商品ID）

**产品明细(去重)表**：
全部去重 listing 详情

### 档位定义

| 档位 | 价格区间 | 含义 |
|------|----------|------|
| 源头直供档 | 价 ≤ Q1 | 价格锚点 |
| 主流走量档 | Q1 < 价 ≤ Q3 | 成交密集，含销量冠军 |
| 精选优价档 | 价 > Q3 | 小批量/高服务/长效期 |

叠加销量标签：爆款领跑 / 稳健供货 / 长尾备选

---

## 六、滑块验证处理机制

### 登录时（auto_login.py）
- CDP 直连（`Input.dispatchMouseEvent`），通过率较高
- 图像分析：numpy 分析每列像素梯度找缺口
- 拖拽：25+步 ease-out 轨迹 + y轴抖动 + 终点回弹
- 失败刷新重试，最多 4 次

### 采集途中（extract.py / fetch_detail.py）
- JS 模拟（`MouseEvent` + `setTimeout`），通过率略低于 CDP 方式
- 检测到易盾滑块 → 先自动解决（3次重试）→ 失败转人工等待（60秒超时）
- fetch_detail.py 检测点：入口页 / 每次导航后 / new_tab兜底后

```
检测到验证弹窗
  ├─ 是易盾滑块？
  │   ├─ 自动解决（最多3次）：
  │   │   1. 等待滑块图片加载
  │   │   2. JS canvas 下载背景图 → 分析每列像素梯度 → 找缺口边缘
  │   │   3. 计算拖拽距离 → setTimeout 调度 MouseEvent 模拟人类拖拽
  │   │   4. 等待3秒 → 检查滑块是否消失
  │   │   5. 失败则刷新验证码重试
  │   └─ 自动解决失败 → 等待人工完成（每2秒检测，超时60秒）
  └─ 其他验证码 → 直接等待人工完成
```

---

## 七、核心设计原则

### 数据准确性（红线）
- 价格不信任 DOM 文本（字体反爬），只信解码后的 priceToken + 详情页互验
- 销量不简单求和（同一商品多个报价档位各有销量），每款商品取最高已拼后汇总
- 卡片与 Vuex 列表按商品名匹配，不按索引对齐（避免推荐商品串号）

### 反爬对抗
- 字体反爬 + 数据层加密 → protobuf 解码绕过
- 采集途中滑块 → 先自动解决再转人工
- 商品间隔 2 秒 + SPA 内导航（不频繁刷新页面）

### 诚实降级
- 详情页抓取失败的行在报表中置灰标「待补采」
- 顶部显示数据完整性状态条（已采集 N / 总 M 条）
- 缺失指标显示「—」，不伪造数据

---

## 八、常见问题

| 问题 | 原因 | AI 如何处理 |
|------|------|------------|
| 列表返回 0 条 | 登录态过期 | 自动检测并重新登录 |
| 所有页数据相同 | URL 的 `?page=N` 对 SPA 无效 | 已内置点击「下一页」按钮翻页 |
| 详情页数据全相同 | SPA 缓存未清除 | 每次导航前先回 `/home`（已内置） |
| 商品链接打不开 | 路由参数用错 | 根据商品名自动选择拼团/普通参数（已内置） |
| 价格显示乱码 | 字体反爬 | 正常现象，真实价格在 priceToken 中 |
| 文件写入后找不到 | browser-use 沙箱隔离 | 采集脚本只 print 到 stdout，由普通 python 写文件 |
| 采集途中频繁弹滑块 | 请求频率过高 | 已内置 2 秒间隔 + 自动解决，仍频繁提示用户手动完成 |

---

## 九、目录结构

```
ysb-wholesale-scraper/
├── SKILL.md                         # 技能说明文档（AI agent 读取）
├── profile.json                     # 成长记录 v2（运行次数/品牌/耗时/质量/趋势/配置记忆）
├── references/
│   ├── decryption.md                # 价格解密原理（字体反爬 + priceToken）
│   ├── vuex_schema.md               # Vuex 数据结构与采集陷阱
│   ├── report_schema.md             # 字段→HTML元素映射表
│   └── troubleshooting.md           # 常见问题排查
└── scripts/
    ├── run.py                       # 一键全流程编排（推荐入口）
    ├── ysb_common.py                # 公共模块：CDP通信 + 滑块验证
    ├── ysb_parser.py                # 公共模块：价格解码 + 销量解析 + JS模板
    ├── auto_login.py                # 自动登录 + 滑块验证
    ├── cdp_extract.py               # 列表页采集（CDP直连，推荐）
    ├── extract.py                   # 列表页采集（browser-use 版，备选）
    ├── cdp_fetch_detail_v2.py       # 详情页采集 v2（CDP直连，推荐）
    ├── fetch_detail.py              # 详情页采集 v1（browser-use 版，备选）
    ├── process.py                   # 解析出表（普通 Python）
    └── grow.py                      # 成长报告查看
```

---

## 十、成长机制（增强版 v2）

每次 `process.py` 出表后，自动把运行记录写入 `profile.json`（v2），下次运行时自动打印成长摘要和趋势提示。

### 自动记录内容

| 维度 | 字段 | 说明 |
|------|------|------|
| 运行基础 | 日期、品牌、采集条数、产品数、TOP1名称+销量 | 基础信息 |
| 运行耗时 | elapsed_sec | 每次记录，品牌级统计平均/总耗时 |
| 数据质量 | 采集成功率、已采集数、待补采数 | 追踪详情页采集完整性 |
| 质量历史 | quality_history 数组 | 采集率变化趋势 |
| 配置记忆 | last_config | 上次运行参数，下次自动提示 |
| 价格趋势 | 对比 sales_history 快照 | TOP1 销量和最低价变化方向+幅度 |

### 智能去重与限流

- **同日同品牌去重**：同一天同一品牌的多次运行只保留最后一次，避免调试运行污染历史
- **runs 上限**：保留最近 200 条，更旧的自动归档到 `archived_runs`

### 成长摘要（process.py 输出末尾自动打印）

```
[grow] 上次运行品牌: 妇炎洁（当前: 云南白药牙膏）
[grow] 上次配置: brand=妇炎洁, pages=15
[grow] 第 3 次运行 · 累计采集 156 条 · 耗时 12.3s · 常用: 妇炎洁(2), 汤臣倍健(1)
[grow] 数据质量: 55/60 已采集(91.7%) · 待补采 5
[grow] 妇炎洁 已跑 2 次 · 上次 48 条/29 个产品 · 平均耗时 11.5s · 最佳质量 95.0%
[trend] TOP1 销量变化: +1200 · 最低价变化: -0.50（对比 2026-07-28）
```

### 查看成长报告

用户只需用自然语言询问，AI 自动运行 `grow.py` 对应模式：

| 用户说 | AI 运行 | 输出内容 |
|--------|---------|----------|
| "看看采集汇总" | `grow.py`（默认） | 全局统计 + 品牌排行 + 质量趋势 + 价格快报 |
| "云南白药牙膏的历史" | `grow.py --brand 云南白药牙膏` | 该品牌历次运行明细 |
| "最近10次运行" | `grow.py --runs 10` | 最近运行简表 |
| "价格趋势分析" | `grow.py --trend` | TOP产品价格/销量趋势对比 |
| "数据质量怎么样" | `grow.py --quality` | 采集率变化追踪表 |
| "生成成长报告" | `grow.py --html` | HTML 可视化报告 |

### profile.json 结构（v2）

```json
{
  "version": 2,
  "first_run": "2026-07-28",
  "last_run": "2026-07-29",
  "total_runs": 10,
  "total_items": 576,
  "brands": {
    "云南白药牙膏": {
      "runs": 5, "first_run": "...", "last_run": "...",
      "last_result": {"items": 60, "products": 10, "top1_name": "...", "top1_sales": 322856},
      "total_items": 300,
      "best_quality": 91.7, "avg_elapsed": 12.3, "total_elapsed": 61.5
    }
  },
  "runs": [
    {
      "date": "2026-07-29T09:16:30",
      "brand": "云南白药牙膏",
      "elapsed_sec": 12.3,
      "result": {"items": 60, "products": 10, "top1_name": "...", "top1_sales": 322856},
      "quality": {"total_wids": 60, "detail_collected": 55, "detail_pending": 5, "collection_rate": 91.7},
      "config": {"brand": "云南白药牙膏", "input": "vuex_raw.json", "has_detail": true}
    }
  ],
  "quality_history": [
    {"date": "...", "brand": "...", "rate": 91.7, "collected": 55, "pending": 5}
  ],
  "last_config": {"brand": "云南白药牙膏", "input": "vuex_raw.json", "has_detail": true},
  "archived_runs": []
}
```

`profile.json` 位于 skill 目录，跨项目共享——不管在哪个工作区跑，成长记录都累积到同一份。
